[project]
name = "polos-hello-world"
version = "0.1.0"
description = "Hello World example for Polos SDK"
requires-python = ">=3.10"
dependencies = [
    "polos-sdk[openai]",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]

[project]
name = "polos-suspend-resume"
version = "0.1.0"
description = "Suspend and resume example for Polos SDK"
requires-python = ">=3.10"
dependencies = [
    "polos-sdk",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]

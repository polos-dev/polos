[project]
name = "polos-error-handling"
version = "0.1.0"
description = "Error handling example for Polos SDK"
requires-python = ">=3.10"
dependencies = [
    "polos-sdk",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]
[project]
name = "polos-router-coordinator"
version = "0.1.0"
description = "Router coordinator example for Polos SDK"
requires-python = ">=3.10"
dependencies = [
    "polos-sdk[openai]",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]

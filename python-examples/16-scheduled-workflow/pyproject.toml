[project]
name = "polos-scheduled-workflow"
version = "0.1.0"
description = "Scheduled workflow example for Polos SDK"
requires-python = ">=3.10"
dependencies = [
    "polos-sdk",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]

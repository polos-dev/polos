[project]
name = "polos-sandbox-tools"
version = "0.1.0"
description = "Agent with sandbox tools running inside a Docker container"
requires-python = ">=3.10"
dependencies = [
    "polos-sdk[anthropic]",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.1.0",
]

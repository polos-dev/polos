{
  "name": "polos-exec-security",
  "version": "0.1.0",
  "description": "Exec security example — allowlist + interactive terminal approval for sandbox commands",
  "private": true,
  "type": "module",
  "scripts": {
    "worker": "tsx worker.ts",
    "main": "tsx main.ts"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.0",
    "@polos/sdk": "latest",
    "dotenv": "^16.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}

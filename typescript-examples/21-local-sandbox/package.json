{
  "name": "polos-local-sandbox",
  "version": "0.1.0",
  "description": "Agent with sandbox tools running locally on the host (no Docker)",
  "private": true,
  "type": "module",
  "scripts": {
    "worker": "tsx worker.ts",
    "main": "tsx main.ts"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.0",
    "@polos/sdk": "latest",
    "dotenv": "^16.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}

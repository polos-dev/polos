# Polos configuration
POLOS_PROJECT_ID=your-project-id
POLOS_API_URL=http://localhost:8080
POLOS_API_KEY=
POLOS_DEPLOYMENT_ID=slack-channel-example

# Slack configuration
# Bot User OAuth Token from your Slack app's OAuth & Permissions page
SLACK_BOT_TOKEN=xoxb-your-bot-token
# Prefer channel IDs (C0123456789) over names — stable across renames
SLACK_CHANNEL=C0123456789
# Signing Secret — from your Slack app's Basic Information page.
# Set this on the orchestrator (not the worker) to enable interactive buttons.
# Without it, buttons still render but clicks will return 500.
SLACK_SIGNING_SECRET=your-signing-secret

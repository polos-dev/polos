{
  "name": "polos-session-sandbox",
  "version": "0.1.0",
  "description": "Session sandbox example — reuse a Docker sandbox across multiple agent runs with the same sessionId",
  "private": true,
  "type": "module",
  "scripts": {
    "main": "tsx main.ts"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.0",
    "@polos/sdk": "../../sdk/typescript",
    "dotenv": "^16.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
